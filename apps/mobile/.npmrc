# O expo-router puxa um react-dom que conflita com a versão fixa do react.
# Este modo ignora esses conflitos de peer dependency, tanto na sua máquina
# quanto no EAS Build, que executa `npm ci` respeitando este arquivo.
legacy-peer-deps=true
