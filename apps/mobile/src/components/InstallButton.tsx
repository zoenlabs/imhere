import React from 'react';

/**
 * No app nativo não existe "instalar": ele já está instalado.
 * A versão que aparece é a InstallButton.web.tsx.
 */
export function InstallButton() {
  return null;
}
